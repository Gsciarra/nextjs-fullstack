{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-all": "pnpm type-check && pnpm lint && pnpm run format && pnpm build"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "eslint": "^8",
    "prettier": "^3",
    "prettier-plugin-tailwindcss": "^0.6.2",
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.2.0",
  "engines": {
    "node": ">=18"
  },
  "name": "with-tailwind"
}
